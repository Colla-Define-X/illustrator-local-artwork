interface:
  display_name: "Illustrator 局部图案"
  short_description: "按页面隔离版本并在同级 aicreate 图层完成局部替换"
  default_prompt: "Use $illustrator-local-artwork to locate the exact target object, assign a stable page or target scope, generate one tone-matched replacement, place only it on a scoped sibling aicreate layer, hide only the old object and same-scope versions, and return the updated AI and final preview."
